[project]
name = "labels-api"
version = "1.0.0"
requires-python = ">=3.14"

dependencies = [
    "connections",
    "observability",
    "text-extraction",
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.29.0",
    "pydantic-settings>=2.2.0",
]

[tool.uv.sources]
connections = { workspace = true }
observability = { workspace = true }
text-extraction = { workspace = true }

[tool.uv]
package = false
