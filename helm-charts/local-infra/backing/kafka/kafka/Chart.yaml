apiVersion: v2
name: kafka
description: Local-dev Kafka (KRaft, single controller, PLAINTEXT) wrapping the Bitnami chart.
type: application
version: 0.1.0
appVersion: "4.0.0"
dependencies:
  - name: kafka
    version: 32.4.3
    repository: https://charts.bitnami.com/bitnami
