apiVersion: v2
name: chief-semantic
description: Chief semantic enrichment service — consumes chief-lexical's semantic trigger topic, chunks and embeds via Triton, indexes to chief-semantic.
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: hermes-common
    version: 0.1.0
    repository: file://../../library/hermes-common
